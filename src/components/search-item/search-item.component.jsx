const SearchItem = () => {
    return (
        <li className="view-item">
            <span className="m-icon">location_on</span>
            <div>
                {/* <p className="item-title">${name}</p>
                <p className="label-2 item-subtitle">{state || ""} {country}</p> */}
            </div>
            <a 
                // href={`#/weather?lat=${lat}&lon=${lon}`} className="item-link has-state" aria-label={`${name} weather`}
            />
        </li>
    )
}

export default SearchItem